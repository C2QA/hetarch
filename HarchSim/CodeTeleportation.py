from HarchSim.clock import Clock
from enum import Enum
import qiskit.quantum_info as qi
import numpy as np
from HarchSim import *

class CT_MODULE:
    ENT_DIST = "entanglement distillation module"
    PLUS_STATE = "plus state module"
    CAT_STATE = "cat state module"
    OUTPUT_MEMORY = "output memory module"

class CodeTeleportation():
    def __init__(self):
        pass

